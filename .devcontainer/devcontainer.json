{
  "name": "tiny-adaptive-ai-accelerator-stack",
  "image": "mcr.microsoft.com/devcontainers/cpp:ubuntu-24.04",
  "features": {
    "ghcr.io/devcontainers/features/python:1": {}
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y build-essential clang-format cmake ninja-build git git-lfs python3-pip python3-venv && git lfs install && pip install -r requirements.txt"
}